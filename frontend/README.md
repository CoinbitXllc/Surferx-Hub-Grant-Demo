# SurferX Hub — Frontend (Phase 1 MVP)

This frontend is a **Next.js web interface** for XRPL discovery.
It connects to the backend API and displays public ledger data.

## Responsibilities
- Account lookup (read-only)
- Token registry display
- No transaction signing
- No wallet custody

## Run Locally

```bash
npm install
npm run dev
