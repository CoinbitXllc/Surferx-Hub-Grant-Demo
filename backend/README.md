# SurferX Hub — Backend (Phase 1 MVP)

This backend provides **read-only XRPL access** and public registry endpoints
for the SurferX Hub Phase 1 MVP.

## Responsibilities
- Query XRPL public ledger (read-only)
- Serve token and account metadata
- Provide a public project registry
- Maintain non-custodial design

## Requirements
- Node.js 18+
- npm

## Setup

```bash
npm install
