# ============================
# SurferX Hub — Phase 1 (MVP)
# Environment Variables
# ============================

# App
NODE_ENV=development
APP_NAME=surferx-hub-mvp
APP_URL=http://localhost:3000

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:4000

# Backend
PORT=4000

# XRPL Network Settings (READ-ONLY for Phase 1)
# Use an XRPL public RPC endpoint or your own node endpoint.
XRPL_NETWORK=mainnet
XRPL_RPC_URL=https://s1.ripple.com:51234

# Optional: WebSocket endpoint for live ledger events (Phase 1 can omit)
XRPL_WS_URL=wss://s1.ripple.com

# Registry / Metadata (Phase 1)
# Used to point to a JSON registry file or service endpoint
REGISTRY_SOURCE=local
REGISTRY_JSON_PATH=./backend/registry/registry.json

# Logging
LOG_LEVEL=info

# Security
# Phase 1 is non-custodial. No private keys should ever be stored.
# NEVER commit secrets. Keep real values in .env (local only).
